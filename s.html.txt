<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Soksori Links</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      text-align: center;
      background: #faf9f7;
      padding: 40px;
    }
    h1 {
      color: #c94d4d;
    }
    a {
      display: block;
      margin: 10px auto;
      padding: 12px 20px;
      width: 250px;
      border-radius: 8px;
      text-decoration: none;
      color: #d6336c;
      background: #fff5f5;
      border: 1px solid #ffd6d6;
      font-weight: bold;
    }
    a:hover {
      background: #ffeaea;
    }
  </style>
</head>
<body>
  <h1 id="title">Loading...</h1>
  <div id="links"></div>

  <script>
    const response = await fetch('https://docs.google.com/spreadsheets/d/e/2PACX-1vSLRkmhqjKq88NMj7k40gs6M_-cVtr0udNcCLoOfdEw0lb1T-NxyHC6PgW3UO6plZSQ_KmDAazw-WkZ/pub?gid=0&single=true&output=csv');
    const text = await response.text();

    const sheetCsvUrl = 'https://docs.google.com/spreadsheets/d/e/2PACX-1vSLRkmhqjKq88NMj7k40gs6M_-cVtr0udNcCLoOfdEw0lb1T-NxyHC6PgW3UO6plZSQ_KmDAazw-WkZ/pub?gid=0&single=true&output=csv';

    const params = new URLSearchParams(window.location.search);
    const slug = params.get('slug');

    fetch(sheetCsvUrl)
      .then(response => response.text())
      .then(csv => {
        const rows = csv.split('\n').map(r => r.trim().split(','));
        const headers = rows[0];
        const data = rows.slice(1);
        const slugIndex = headers.indexOf('slug');
        const titleIndex = headers.indexOf('title');

        const target = rows.find(r => r[slugIndex] === slug);

        if (target) {
          const titleIndex = headers.indexOf('title');
          document.getElementById('title').innerText = target[titleIndex];

          const linksDiv = document.getElementById('links');
          const platforms = ['spotify', 'apple', 'youtube', 'yandex'];

          platforms.forEach(name => {
            const index = headers.indexOf(name);
            const url = target[index];
            if (url && url.startsWith('http')) {
              const link = document.createElement('a');
              link.href = url;
              link.target = '_blank';
              link.textContent = name.toUpperCase();
              linksDiv.appendChild(link);
            }
          headers.forEach((header, i) => {
      if (header !== 'slug' && header !== 'title' && target[i]) {
        const a = document.createElement('a');
        a.href = target[i];
        a.target = '_blank';
        a.textContent = header.toUpperCase();
        a.style.display = 'block';
        a.style.margin = '10px 0';
        a.style.padding = '12px';
        a.style.background = '#ffeaea';
        a.style.borderRadius = '8px';
        a.style.textDecoration = 'none';
        a.style.color = '#d6336c';
        linksDiv.appendChild(a);
      }
          });
        } else {
          document.getElementById('title').innerText = 'Not found 😢';
        }
      })
      .catch(() => {
        document.getElementById('title').innerText = 'Error loading data';
      });
  </script>
</body>
</html>
